namespace Entanglement.Network {
    public class NetworkInfo {
        // TODO: actually make this check if your in a lobby or not
        public static bool hasLobby {
            get {
                return true;
            }
            protected set { }
        }
    }
}